isValid
時間: O(n)
空間: O(n)

把遇到的左括號都push進stack中，每次遇到右括號時從stack pop一個元素出來，這個元素必須是對應到的左括號，以下情況會回傳False
    1. pop時，左右括號類型不同，例如："{]"
    2. pop時，stack為空，代表右括號太多，例如："([}"
    3. 結束走訪整個字串時，發現stack不為空，代表左括號太多，例如："([]"